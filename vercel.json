{
  "buildCommand": "pnpm build && node vercel-deploy.js",
  "outputDirectory": ".vercel/output",
  "framework": "astro",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}