---
import Layout from '../layouts/Layout.astro';
import { personalInfo, education, certifications, workExperience, startup, skills, languages, strengths } from '../data/info.ts';
---

<Layout title="Sobre Mí - Nicolas Moreno" description="Conoce más sobre Nicolas Moreno, desarrollador backend especializado en tecnologías modernas y fundador de TIKNO.">
  <!-- Hero Section -->
  <section class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 relative overflow-hidden">
    <!-- Fondo animado -->
    <div class="absolute inset-0 bg-gradient-to-br from-gray-900 via-gray-800/50 to-gray-900">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_30%_20%,rgba(156,163,175,0.15),transparent_60%)] animate-pulse"></div>
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_70%_80%,rgba(156,163,175,0.1),transparent_60%)] animate-pulse delay-1000"></div>
    </div>
    
    <!-- Partículas flotantes -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
      <div class="absolute top-1/4 left-1/4 w-2 h-2 bg-gray-400 rounded-full animate-ping delay-300"></div>
      <div class="absolute top-3/4 right-1/4 w-1 h-1 bg-gray-300 rounded-full animate-ping delay-700"></div>
      <div class="absolute top-1/2 left-3/4 w-1.5 h-1.5 bg-gray-500 rounded-full animate-ping delay-1000"></div>
      <div class="absolute top-1/3 right-1/3 w-1 h-1 bg-gray-200 rounded-full animate-ping delay-1500"></div>
    </div>
    
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
        <!-- Contenido principal -->
        <div class="space-y-8 animate-fade-in">
          <div class="space-y-4">
            <h1 class="text-5xl md:text-7xl font-bold leading-tight">
              Sobre <span class="text-gray-300 animate-pulse">Mí</span>
            </h1>
            <p class="text-xl text-gray-300 leading-relaxed">
              {personalInfo.bio}
            </p>
          </div>
          
          <!-- Información básica -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="bg-gray-800/50 backdrop-blur-sm border border-gray-700/50 rounded-xl p-6 hover:border-gray-500/50 transition-all duration-500 group">
              <div class="flex items-center space-x-3 mb-3">
                <div class="w-3 h-3 bg-gray-500 rounded-full animate-pulse"></div>
                <h3 class="text-lg font-semibold text-gray-400">Ubicación</h3>
              </div>
              <p class="text-gray-300">{personalInfo.location}</p>
            </div>
            
            <div class="bg-gray-800/50 backdrop-blur-sm border border-gray-700/50 rounded-xl p-6 hover:border-gray-500/50 transition-all duration-500 group">
              <div class="flex items-center space-x-3 mb-3">
                <div class="w-3 h-3 bg-gray-400 rounded-full animate-pulse"></div>
                <h3 class="text-lg font-semibold text-gray-400">Estado</h3>
              </div>
              <p class="text-gray-300">Disponible para proyectos</p>
            </div>
          </div>
        </div>
        
        <!-- Avatar mejorado -->
        <div class="flex justify-center lg:justify-end">
          <div class="relative w-80 h-80">
            <div class="absolute inset-0 bg-gradient-to-br from-gray-500/20 to-gray-700/20 rounded-full blur-2xl animate-pulse"></div>
            <div class="relative w-full h-full bg-gray-900 rounded-full overflow-hidden border-4 border-gray-500/30 hover:border-gray-400/60 transition-all duration-700">
              <img 
                src="/Perfil.jpg" 
                alt="Nicolas Moreno" 
                class="w-full h-full object-cover transition-transform duration-700 hover:scale-110"
              />
              <div class="absolute inset-0 bg-gradient-to-br from-gray-500/10 via-transparent to-gray-700/10 opacity-0 hover:opacity-100 transition-opacity duration-500"></div>
            </div>
            
            <!-- Anillos orbitales -->
            <div class="absolute inset-0 animate-spin" style="animation-duration: 30s;">
              <div class="absolute inset-0 border-2 border-gray-500/30 rounded-full">
                <div class="absolute top-0 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-4 h-4 bg-gray-400 rounded-full animate-pulse"></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  

    <!-- Skills Section -->
  <section class="py-20 bg-gradient-to-br from-gray-800 to-gray-900 relative overflow-hidden">
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(156,163,175,0.05),transparent_70%)] animate-pulse"></div>
    
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="text-center mb-16">
        <h2 class="text-4xl md:text-5xl font-bold mb-6">
          Habilidades <span class="text-gray-300">Técnicas</span>
        </h2>
        <div class="w-24 h-1 bg-gradient-to-r from-gray-500 to-gray-700 mx-auto rounded-full"></div>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <!-- Lenguajes de Programación -->
        <div class="bg-gray-800/60 backdrop-blur-sm border border-gray-700/50 rounded-2xl p-8 hover:border-gray-500/50 transition-all duration-500 group">
          <div class="flex items-center space-x-3 mb-6">
            <div class="w-12 h-12 bg-gradient-to-br from-gray-500 to-gray-700 rounded-xl flex items-center justify-center">
              <span class="text-xl">💻</span>
            </div>
            <h3 class="text-xl font-bold text-gray-300">Lenguajes</h3>
          </div>
          <div class="space-y-3">
            {skills.programmingLanguages.map((lang, index) => (
              <div class="flex items-center space-x-3 group/item hover:translate-x-2 transition-transform duration-300">
                <div class="w-2 h-2 bg-gray-400 rounded-full group-hover/item:scale-125 transition-transform duration-300"></div>
                <span class="text-gray-300 group-hover/item:text-gray-200 transition-colors duration-300">{lang.name}</span>
              </div>
            ))}
          </div>
        </div>
        
        <!-- Frontend -->
        <div class="bg-gray-800/60 backdrop-blur-sm border border-gray-700/50 rounded-2xl p-8 hover:border-gray-500/50 transition-all duration-500 group">
          <div class="flex items-center space-x-3 mb-6">
            <div class="w-12 h-12 bg-gradient-to-br from-gray-500 to-gray-700 rounded-xl flex items-center justify-center">
              <span class="text-xl">🎨</span>
            </div>
            <h3 class="text-xl font-bold text-gray-300">Frontend</h3>
          </div>
          <div class="space-y-3">
            {skills.frontend.map((tech, index) => (
              <div class="flex items-center space-x-3 group/item hover:translate-x-2 transition-transform duration-300">
                <div class="w-2 h-2 bg-gray-400 rounded-full group-hover/item:scale-125 transition-transform duration-300"></div>
                <span class="text-gray-300 group-hover/item:text-gray-200 transition-colors duration-300">{tech.name}</span>
              </div>
            ))}
          </div>
        </div>
        
        <!-- Backend -->
        <div class="bg-gray-800/60 backdrop-blur-sm border border-gray-700/50 rounded-2xl p-8 hover:border-gray-500/50 transition-all duration-500 group">
          <div class="flex items-center space-x-3 mb-6">
            <div class="w-12 h-12 bg-gradient-to-br from-gray-500 to-gray-700 rounded-xl flex items-center justify-center">
              <span class="text-xl">⚙️</span>
            </div>
            <h3 class="text-xl font-bold text-gray-300">Backend</h3>
          </div>
          <div class="space-y-3">
            {skills.backend.map((tech, index) => (
              <div class="flex items-center space-x-3 group/item hover:translate-x-2 transition-transform duration-300">
                <div class="w-2 h-2 bg-gray-400 rounded-full group-hover/item:scale-125 transition-transform duration-300"></div>
                <span class="text-gray-300 group-hover/item:text-gray-200 transition-colors duration-300">{tech.name}</span>
              </div>
            ))}
          </div>
        </div>
        
        <!-- Bases de Datos -->
        <div class="bg-gray-800/60 backdrop-blur-sm border border-gray-700/50 rounded-2xl p-8 hover:border-gray-500/50 transition-all duration-500 group">
          <div class="flex items-center space-x-3 mb-6">
            <div class="w-12 h-12 bg-gradient-to-br from-gray-500 to-gray-700 rounded-xl flex items-center justify-center">
              <span class="text-xl">🗄️</span>
            </div>
            <h3 class="text-xl font-bold text-gray-300">Bases de Datos</h3>
          </div>
          <div class="space-y-3">
            {skills.databases.map((db, index) => (
              <div class="flex items-center space-x-3 group/item hover:translate-x-2 transition-transform duration-300">
                <div class="w-2 h-2 bg-gray-400 rounded-full group-hover/item:scale-125 transition-transform duration-300"></div>
                <span class="text-gray-300 group-hover/item:text-gray-200 transition-colors duration-300">{db.name}</span>
              </div>
            ))}
          </div>
        </div>
        
        <!-- Infraestructura -->
        <div class="bg-gray-800/60 backdrop-blur-sm border border-gray-700/50 rounded-2xl p-8 hover:border-gray-500/50 transition-all duration-500 group">
          <div class="flex items-center space-x-3 mb-6">
            <div class="w-12 h-12 bg-gradient-to-br from-gray-500 to-gray-700 rounded-xl flex items-center justify-center">
              <span class="text-xl">☁️</span>
            </div>
            <h3 class="text-xl font-bold text-gray-300">Infraestructura</h3>
          </div>
          <div class="space-y-3">
            {skills.infrastructure.map((infra, index) => (
              <div class="flex items-center space-x-3 group/item hover:translate-x-2 transition-transform duration-300">
                <div class="w-2 h-2 bg-gray-400 rounded-full group-hover/item:scale-125 transition-transform duration-300"></div>
                <span class="text-gray-300 group-hover/item:text-gray-200 transition-colors duration-300">{infra.name}</span>
              </div>
            ))}
          </div>
        </div>
        
        <!-- Idiomas -->
        <div class="bg-gray-800/60 backdrop-blur-sm border border-gray-700/50 rounded-2xl p-8 hover:border-gray-500/50 transition-all duration-500 group">
          <div class="flex items-center space-x-3 mb-6">
            <div class="w-12 h-12 bg-gradient-to-br from-gray-500 to-gray-700 rounded-xl flex items-center justify-center">
              <span class="text-xl">🌍</span>
            </div>
            <h3 class="text-xl font-bold text-gray-300">Idiomas</h3>
          </div>
          <div class="space-y-3">
            {languages.map((lang, index) => (
              <div class="flex items-center justify-between group/item hover:translate-x-2 transition-transform duration-300">
                <div class="flex items-center space-x-3">
                  <div class="w-2 h-2 bg-gray-400 rounded-full group-hover/item:scale-125 transition-transform duration-300"></div>
                  <span class="text-gray-300 group-hover/item:text-gray-200 transition-colors duration-300">{lang.name}</span>
                </div>
                <span class="text-sm text-gray-300 font-semibold">{lang.level}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  
  <!-- Experiencia Laboral -->
  <section class="py-20 bg-gradient-to-br from-gray-900 to-gray-800 relative">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-4xl md:text-5xl font-bold mb-6">
          Experiencia <span class="text-gray-300">Laboral</span>
        </h2>
        <div class="w-24 h-1 bg-gradient-to-r from-gray-500 to-gray-700 mx-auto rounded-full"></div>
      </div>
      
      <div class="space-y-8">
        {workExperience.map((job, index) => (
          <div class="bg-gray-800/60 backdrop-blur-sm border border-gray-700/50 rounded-2xl p-8 hover:border-gray-500/50 transition-all duration-500 group">
            <div class="grid grid-cols-1 lg:grid-cols-4 gap-6">
              <div class="lg:col-span-1">
                <div class="space-y-2">
                  <h3 class="text-xl font-bold text-gray-300">{job.position}</h3>
                  <p class="text-lg font-semibold text-gray-200">{job.company}</p>
                  <p class="text-sm text-gray-400">{job.period}</p>
                  <span class="inline-block px-3 py-1 bg-gray-500/20 text-gray-300 rounded-full text-xs font-semibold">
                    {job.type}
                  </span>
                </div>
              </div>
              
              <div class="lg:col-span-3">
                <p class="text-gray-300 leading-relaxed">{job.description}</p>
                {job.location && (
                  <p class="text-sm text-gray-400 mt-3">📍 {job.location}</p>
                )}
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>


  <!-- Startup Section -->
  <section class="py-20 bg-gray-900 relative overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-r from-gray-900/10 to-transparent"></div>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="text-center mb-16">
        <h2 class="text-4xl md:text-5xl font-bold mb-6">
          Mi <span class="text-gray-300">Startup</span>
        </h2>
        <div class="w-24 h-1 bg-gradient-to-r from-gray-500 to-gray-700 mx-auto rounded-full"></div>
      </div>
      
      <div class="bg-gradient-to-br from-gray-800/80 to-gray-900/80 backdrop-blur-sm border border-gray-700/50 rounded-2xl p-8 md:p-12 hover:border-gray-500/50 transition-all duration-700 group">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <div class="space-y-6">
            <div class="flex items-center space-x-4">
              <div class="w-16 h-16 bg-gradient-to-br from-gray-500 to-gray-700 rounded-xl flex items-center justify-center">
                <span class="text-2xl font-bold text-white">{startup.name.charAt(0)}</span>
              </div>
              <div>
                <h3 class="text-3xl font-bold text-gray-300">{startup.name}</h3>
                <p class="text-gray-400">{startup.description}</p>
              </div>
            </div>
            
            <p class="text-lg text-gray-300 leading-relaxed">
              {startup.description_long}
            </p>
            
            <div class="flex items-center space-x-4">
              <span class="px-4 py-2 bg-gray-500/20 text-gray-300 rounded-full text-sm font-semibold">
                {startup.role}
              </span>
              <a href={startup.website} target="_blank" class="text-gray-300 hover:text-gray-200 transition-colors duration-300 font-semibold">
                Ver sitio web →
              </a>
            </div>
          </div>
          
          <div class="space-y-4">
            <h4 class="text-xl font-semibold text-gray-200 mb-6">Enfoque Principal</h4>
            {startup.focus.map((item, index) => (
              <div class="flex items-start space-x-3 group/item hover:translate-x-2 transition-transform duration-300">
                <div class="w-2 h-2 bg-gray-400 rounded-full mt-2 group-hover/item:scale-125 transition-transform duration-300"></div>
                <p class="text-gray-300 group-hover/item:text-gray-200 transition-colors duration-300">{item}</p>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Educación y Certificaciones -->
  <section class="py-20 bg-gray-900 relative">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16">
        <!-- Educación -->
        <div>
          <div class="text-center lg:text-left mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-6">
              <span class="text-gray-300">Educación</span>
            </h2>
            <div class="w-16 h-1 bg-gradient-to-r from-gray-500 to-gray-700 mx-auto lg:mx-0 rounded-full"></div>
          </div>
          
          <div class="space-y-6">
            {education.map((edu, index) => (
              <div class="bg-gray-800/50 backdrop-blur-sm border border-gray-700/50 rounded-xl p-6 hover:border-gray-500/50 transition-all duration-500">
                <div class="flex items-start justify-between mb-3">
                  <h3 class="text-lg font-bold text-gray-300">{edu.degree}</h3>
                  <span class={`px-3 py-1 rounded-full text-xs font-semibold ${
                    edu.status === 'En progreso' 
                      ? 'bg-gray-500/20 text-gray-300' 
                      : 'bg-gray-600/20 text-gray-200'
                  }`}>
                    {edu.status}
                  </span>
                </div>
                <p class="text-gray-200 font-semibold">{edu.institution}</p>
                <p class="text-sm text-gray-400">{edu.period}</p>
                {edu.expectedGraduation && (
                  <p class="text-sm text-gray-300 mt-2">Graduación esperada: {edu.expectedGraduation}</p>
                )}
              </div>
            ))}
          </div>
        </div>
        
        <!-- Certificaciones -->
        <div>
          <div class="text-center lg:text-left mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-6">
              <span class="text-gray-300">Certificaciones</span>
            </h2>
            <div class="w-16 h-1 bg-gradient-to-r from-gray-500 to-gray-700 mx-auto lg:mx-0 rounded-full"></div>
          </div>
          
          <div class="space-y-6">
            {certifications.map((cert, index) => (
              <div class="bg-gray-800/50 backdrop-blur-sm border border-gray-700/50 rounded-xl p-6 hover:border-gray-500/50 transition-all duration-500">
                <h3 class="text-lg font-bold text-gray-300 mb-2">{cert.name}</h3>
                <p class="text-gray-200 font-semibold">{cert.issuer}</p>
                <p class="text-sm text-gray-400">{cert.date || cert.data}</p>
                <span class="inline-block px-3 py-1 bg-gray-500/20 text-gray-300 rounded-full text-xs font-semibold mt-3">
                  {cert.type}
                </span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>
  
  
  <!-- Fortalezas -->
  <section class="py-20 bg-gray-900 relative">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-4xl md:text-5xl font-bold mb-6">
          Mis <span class="text-gray-300">Fortalezas</span>
        </h2>
        <div class="w-24 h-1 bg-gradient-to-r from-gray-500 to-gray-700 mx-auto rounded-full"></div>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {strengths.map((strength, index) => (
          <div class="bg-gradient-to-br from-gray-800/80 to-gray-900/80 backdrop-blur-sm border border-gray-700/50 rounded-xl p-6 hover:border-gray-500/50 transition-all duration-500 group hover:scale-105">
            <div class="flex items-start space-x-4">
              <div class="w-6 h-6 bg-gradient-to-br from-gray-500 to-gray-700 rounded-full flex items-center justify-center flex-shrink-0 mt-1">
                <span class="text-white text-sm font-bold">✓</span>
              </div>
              <p class="text-gray-300 group-hover:text-gray-200 transition-colors duration-300 leading-relaxed">
                {strength}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
  
  <!-- CTA Section -->
  <section class="py-20 bg-gradient-to-br from-gray-900/20 to-gray-900 relative overflow-hidden">
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(156,163,175,0.1),transparent_70%)] animate-pulse"></div>
    
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center relative z-10">
      <h2 class="text-4xl md:text-5xl font-bold mb-6">
        ¿Listo para <span class="text-gray-300">colaborar</span>?
      </h2>
      <p class="text-xl text-gray-300 mb-8 leading-relaxed">
        Estoy disponible para nuevos proyectos y oportunidades. 
        ¡Hablemos sobre cómo puedo ayudarte a hacer realidad tus ideas!
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/contact" class="bg-gradient-to-r from-gray-600 to-gray-700 hover:from-gray-500 hover:to-gray-600 text-white px-8 py-4 rounded-lg font-semibold transition-all duration-500 transform hover:scale-105 hover:shadow-2xl hover:shadow-gray-500/40 relative overflow-hidden group">
          <span class="relative z-10">Contactar</span>
          <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/25 to-transparent transform -skew-x-12 -translate-x-full group-hover:translate-x-full transition-transform duration-700"></div>
        </a>
        
        <a href="https://linkedin.com/in/nicolas-moreno-dev" target="_blank" class="border-2 border-gray-600 text-gray-300 hover:bg-gray-600 hover:text-white px-8 py-4 rounded-lg font-semibold transition-all duration-500 transform hover:scale-105 hover:shadow-2xl hover:shadow-gray-500/40">
          Ver LinkedIn
        </a>
      </div>
    </div>
  </section>
</Layout>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  .animate-fade-in {
    animation: fade-in 1s ease-out;
  }
  
  @keyframes glow {
    0%, 100% {
      opacity: 0.5;
      transform: scale(1);
    }
    50% {
      opacity: 1;
      transform: scale(1.05);
    }
  }
  
  .animate-glow {
    animation: glow 2s ease-in-out infinite;
  }
</style>